#!/bin/sh
# run all current benchmarks (defined in Benchmarks.curry)
make
export PATH=/home/sicstus/sicstus4/bin:$PATH # for SICStus benchmarks
export SWIPL=/home/swiprolog/bin/swipl # for SWI-Prolog benchmarks
cd suite && ../Benchmarks.state $*
